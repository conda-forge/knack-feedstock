{% set name = "knack" %}
{% set version = "0.7.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 004e4afb8c77828d55b8115a58ed7e30fde2886d682e26028a75609cff2d1731

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - argcomplete
    - colorama
    - jmespath
    - pygments
    - pyyaml
    - six
    - tabulate

test:
  imports:
    - knack
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/microsoft/knack
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A Command-Line Interface framework

  doc_url: https://github.com/Microsoft/knack/tree/master/docs
  dev_url: https://github.com/microsoft/knack

extra:
  recipe-maintainers:
    - awvwgk
    - dhirschfeld
    - andreyz4k
